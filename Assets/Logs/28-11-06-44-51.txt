LOG GENERATED FOR THE SESSION

Max Tokens : 500
Temperature: 0,5
Presence Penalty: 0,1
Frequency Penalty: 0,1
Model: text-davinci-003


You wrote the following  sentence : find the gameobjects called Model_1,Model_2 and Model_3, and change them with the models inside the folder called Models
​

The script generated by the AI is the following 

using UnityEngine;

public class ModelChange : MonoBehaviour
{
    public GameObject[] models;

    private void Start()
    {
        models = Resources.LoadAll<GameObject>("Models");

        foreach (GameObject model in models)
        {
            if (model.name == "Model_1")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_2")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_3")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
        }
    }
}
Elapsed time for the generation of the script : 3,876572 seconds
You wrote the following  sentence : find the gameobjects called Model_1,Model_2 and Model_3, and change them into the models inside the folder called Models
​

The script generated by the AI is the following 

using UnityEngine;

public class ModelChange : MonoBehaviour
{
    public GameObject[] models;

    private void Start()
    {
        models = Resources.LoadAll<GameObject>("Models");

        foreach (GameObject model in models)
        {
            if (model.name == "Model_1")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_2")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_3")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
        }
    }
}
Elapsed time for the generation of the script : 4,489899 seconds
You wrote the following  sentence : find the gameobjects called Model_1,Model_2 and Model_3, and change them into the models inside the folder called Models
​

The script generated by the AI is the following 

using UnityEngine;

public class ModelChange : MonoBehaviour
{
    public GameObject[] models;

    private void Start()
    {
        models = Resources.LoadAll<GameObject>("Models");

        foreach (GameObject model in models)
        {
            if (model.name == "Model_1")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_2")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
            else if (model.name == "Model_3")
            {
                Instantiate(model, transform.position, transform.rotation);
            }
        }
    }
}
Elapsed time for the generation of the script : 4,489899 seconds
You wrote the following  sentence : find the gameobjects called Model_1,Model_2 and Model_3, and change them into the models inside the folder called Models
​

The script generated by the AI is the following 

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChangeModels : MonoBehaviour
{
    public string folderName = "Models";

    void Start()
    {
        //Find the gameobjects called Model_1,Model_2 and Model_3
        GameObject model1 = GameObject.Find("Model_1");
        GameObject model2 = GameObject.Find("Model_2");
        GameObject model3 = GameObject.Find("Model_3");

        //Change them into the models inside the folder called Models
        model1.GetComponent<MeshFilter>().mesh = Resources.Load<Mesh>(folderName + "/Model_1");
        model2.GetComponent<MeshFilter>().mesh = Resources.Load<Mesh>(folderName + "/Model_2");
        model3.GetComponent<MeshFilter>().mesh = Resources.Load<Mesh>(folderName + "/Model_3");
    }
}
Elapsed time for the generation of the script : 4,489899 seconds